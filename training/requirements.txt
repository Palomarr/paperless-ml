# HTR fine-tune deps — pair with Dockerfile (CUDA 11.8 base).
# Pinned conservatively to match ml-gateway's `Dockerfile.fastapi-ort` so
# ONNX exports produced here load cleanly in the serving container.

torch==2.2.2
transformers==4.49.0
accelerate==0.34.2
datasets==3.1.0
sentence-transformers==3.2.0
sentencepiece==0.2.0
protobuf==5.28.3
safetensors==0.4.5

# ONNX export
optimum[onnxruntime-gpu]==1.23.3
onnx==1.17.0

# Data I/O
minio==7.2.12
pyarrow==18.1.0
Pillow==11.0.0
numpy==1.26.4

# Metrics
jiwer==3.0.5

# MLflow
mlflow==2.17.2
